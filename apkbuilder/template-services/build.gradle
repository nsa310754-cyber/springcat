plugins {
    id 'com.android.application' version '8.6.0' apply false
    // 2.2+ needed: some Firebase/Play Services transitive libs (play-services-measurement)
    // ship Kotlin 2.2 metadata that older compilers can't read.
    id 'org.jetbrains.kotlin.android' version '2.2.0' apply false
}
