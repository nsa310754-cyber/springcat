# Copy to .env and fill in a DNS name that already points at this host's
# public IP. Caddy uses it to request a Let's Encrypt certificate
# automatically, so wss:// works without any manual TLS setup.
DOMAIN=screenlink.example.com
