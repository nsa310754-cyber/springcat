# Gradle / Android build artifacts
.gradle/
build/
app/build/
*.iml
.idea/
captures/
.externalNativeBuild/
.cxx/

# ローカル環境依存 (各自の SDK パスを指す)
local.properties

# アイコン生成の中間出力（res/ と dist/ に配置済みのものが正）
icon/out/
