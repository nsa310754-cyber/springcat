<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- ゲーム背景(水色→ピンク)。起動時の黒フラッシュ防止と、
         システムバーをゲーム配色に合わせるための色。 -->
    <color name="game_bg">#F2A0F1</color>
    <color name="game_top">#A0D8F1</color>

    <!-- ActionBar 無しの通常テーマ。システムUI(ステータス/ナビ)は表示する。 -->
    <style name="AppTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/game_bg</item>
        <item name="android:statusBarColor">@color/game_top</item>
        <item name="android:navigationBarColor">@color/game_bg</item>
    </style>
</resources>
