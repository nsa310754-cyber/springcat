plugins {
    id 'com.android.application'
}

android {
    namespace 'site.ragdollp.filemanager'
    compileSdk 35

    defaultConfig {
        applicationId "site.ragdollp.filemanager"
        minSdk 24
        targetSdk 34
        versionCode 2
        versionName "1.1.0"
    }

    signingConfigs {
        release {
            // 個人配布/提出用の使い捨て鍵。Play Store 公開時は各自の鍵に差し替えること。
            // パスワードはゲームアプリと共通 (README 参照)。
            storeFile file("../release.keystore")
            storePassword "blockdestory"
            keyAlias "blockdestory"
            keyPassword "blockdestory"
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    // UI 本体 (app.html) は再圧縮不要。
    aaptOptions {
        noCompress 'html'
    }

    packagingOptions {
        resources {
            excludes += ['META-INF/LICENSE*', 'META-INF/NOTICE*', 'META-INF/*.kotlin_module']
        }
    }
}

dependencies {
    implementation 'androidx.core:core:1.13.1'
    implementation 'androidx.webkit:webkit:1.11.0'
    implementation 'androidx.documentfile:documentfile:1.0.1'

    // 7z/tar/gz の生成・展開 (7z のパスワード読取も対応)。
    implementation 'org.apache.commons:commons-compress:1.27.1'
    // 7z(LZMA/LZMA2) 用。
    implementation 'org.tukaani:xz:1.10'
    // rar 展開用 (rar の作成は独自形式のため非対応。展開のみ。パスワード対応)。
    implementation 'com.github.junrar:junrar:7.5.5'
    // パスワード付き zip の作成/展開 (AES-256)。標準 zip も扱う。
    implementation 'net.lingala.zip4j:zip4j:2.11.5'
}
